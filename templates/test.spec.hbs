
/**
 * @description {{title}} 
 * @see {{link}}
 */
import { describe, expect, test } from 'vitest'
import { {{name}} } from '.'

describe('{{title}}', () => {
  test(`输入： 输出：`, () => {
    expect({{name}}()).toBe()
  })
})